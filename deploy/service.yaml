apiVersion: v1
kind: Service
metadata:
  name: trade-itg-service
  namespace: pay-ns
  labels:
    app: trade-itg
spec:
  type: NodePort
  selector:
    app: trade-itg
  ports:
  - name: rpc
    port: 8080
    targetPort: 8080
    nodePort: 30881
    protocol: TCP
